# Engramly CLI

Authenticate with `engram auth set <api-key>`, then use `engram pdf inspect`, `read`, `search`, or `parse`.
Long-running PDF commands report preparation and parsing phases on stderr, so
redirected Markdown or `--json` stdout remains machine-readable.
